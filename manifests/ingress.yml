apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: akskodey
    labels:
        app: akskodey
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: akskodey495-akskodey.
          http:
            paths:
            - path: /
              backend:
                serviceName: akskodey
                servicePort: 80