apiVersion : apps/v1beta1
kind: Deployment
metadata:
  name: kpwithoutingress
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: kpwithoutingress
    spec:
      containers:
        - name: kpwithoutingress
          image: kapasrijkubacr.azurecr.io/kpwithoutingress
          ports:
          - containerPort: 8080